<h1>コメント一覧</h1>

<% @comments.each do |comment| %>
  <div>
    <p><%= comment.body %></p>
    <small><%= comment.created_at.strftime("%Y-%m-%d %H:%M") %></small>
    <%= link_to "詳細", comment_path(comment) %>
  </div>
<% end %>

<%= link_to "記事に戻る", article_path(@article) %>
